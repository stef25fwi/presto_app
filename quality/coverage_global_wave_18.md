# Global coverage wave 18

## Scope

This wave targets pure, previously uncovered helpers to progress toward 25% global LCOV without lowering thresholds or excluding files.

Covered files:

- `lib/services/profile_department_resolver.dart`;
- `lib/features/admin_videomaker/video_maker_models.dart`.

Covered paths include department normalization and resolution, DOM/city/postal fallbacks, display labels, generated video parsing, MIME detection, byte/date formatting, and map normalization.

## Measurement rule

No final coverage percentage is declared before the pull-request validation produces a complete LCOV artifact.
