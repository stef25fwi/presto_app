name: Temporary messaging coverage executor

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: write

jobs:
  implement-and-test:
    if: github.head_ref == 'coverage/messaging-thread-massive-wave' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.6
          channel: stable
          cache: true

      - name: Apply zero-line-growth messaging testability refactor
        run: python3 tools/temp_messaging_thread_public_helpers_patch.py

      - name: Format changed Dart files
        run: dart format lib/pages/messages/conversation_thread_page.dart test/pages/messages/conversation_thread_pure_helpers_massive_coverage_test.dart

      - name: Enforce legacy page line budget
        shell: bash
        run: test "$(wc -l < lib/pages/messages/conversation_thread_page.dart)" -le 5182

      - name: Dependencies
        run: flutter pub get

      - name: Targeted messaging helper tests
        shell: bash
        run: |
          set -o pipefail
          flutter test --reporter expanded test/pages/messages/conversation_thread_pure_helpers_massive_coverage_test.dart 2>&1 | tee messaging-helper-test.log

      - name: Upload messaging test diagnostic
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: messaging-helper-diagnostic-${{ github.run_id }}
          path: messaging-helper-test.log
          retention-days: 2

      - name: Targeted analyzer
        run: flutter analyze lib/pages/messages/conversation_thread_page.dart test/pages/messages/conversation_thread_pure_helpers_massive_coverage_test.dart

      - name: Commit validated implementation and clean executor
        shell: bash
        run: |
          rm -f .github/workflows/temp-export-messaging-thread.yml
          rm -f tools/temp_messaging_thread_public_helpers_patch.py
          rm -f messaging-helper-test.log
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "test(coverage): cover messaging parsing and attachment helpers"
          git push origin HEAD:${{ github.head_ref }}
