name: Temporary repair PR694

on:
  push:
    branches: [main]
    paths:
      - tools/repair_pr694.py
      - .github/workflows/temporary-repair-pr694.yml
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

concurrency:
  group: temporary-repair-pr694
  cancel-in-progress: false

jobs:
  repair:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v7
        with:
          ref: coverage/w1-messaging-next-20260723
          fetch-depth: 0

      - name: Apply deterministic repair
        shell: bash
        run: |
          set -euo pipefail
          git fetch origin \
            +refs/heads/main:refs/remotes/origin/main \
            +refs/heads/coverage/w1-messaging-next-20260723:refs/remotes/origin/coverage/w1-messaging-next-20260723
          git switch -C coverage/w1-messaging-next-20260723 \
            refs/remotes/origin/coverage/w1-messaging-next-20260723

          git show origin/main:tools/repair_pr694.py > /tmp/repair_pr694.py
          python3 /tmp/repair_pr694.py

          git diff --check
          if git diff --quiet; then
            echo 'Repair already applied.'
            exit 0
          fi

          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m 'fix(messages): defer admin log UI mutation'
          git push origin HEAD:coverage/w1-messaging-next-20260723
