name: Validate and Deploy Firebase

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: firebase-production
  cancel-in-progress: false

jobs:
  validate-and-deploy:
    environment: recaptcha
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Decode Google Credentials
        env:
          GOOGLE_CREDENTIALS_B64: ${{ secrets.GOOGLE_CREDENTIALS_B64 }}
        run: |
          if [ -z "$GOOGLE_CREDENTIALS_B64" ]; then
            echo "❌ GOOGLE_CREDENTIALS_B64 est vide. Ajoute ce secret dans l'environnement GitHub recaptcha."
            exit 1
          fi

          printf "%s" "$GOOGLE_CREDENTIALS_B64" \
            | tr -d '\r\n[:space:]' \
            | base64 -d > "$RUNNER_TEMP/google-credentials.json"

          python3 -m json.tool "$RUNNER_TEMP/google-credentials.json" >/dev/null
          echo "✅ Google credentials JSON décodé et validé"

          echo "GOOGLE_CREDENTIALS_JSON<<HEREDOC_EOF" >> "$GITHUB_ENV"
          cat "$RUNNER_TEMP/google-credentials.json" >> "$GITHUB_ENV"
          echo "HEREDOC_EOF" >> "$GITHUB_ENV"

      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: '${{ env.GOOGLE_CREDENTIALS_JSON }}'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Enable Flutter web
        run: flutter config --enable-web

      - name: Install dependencies
        run: flutter pub get

      - name: Analyze Flutter
        run: flutter analyze --fatal-infos

      - name: Test Flutter
        run: flutter test

      - name: Install Functions dependencies
        run: npm --prefix functions ci

      - name: Build Functions
        run: npm --prefix functions run build

      - name: Test Functions
        run: npm --prefix functions test

      - name: Build Web for Firebase Hosting
        env:
          APPCHECK_RECAPTCHA_SITE_KEY: ${{ secrets.APPCHECK_RECAPTCHA_SITE_KEY }}
        run: |
          bash tools/flutter_with_build_stamp.sh build web --release --no-wasm-dry-run

      - name: Setup Node.js 20 for Firebase CLI
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Firebase CLI
        run: npm install -g firebase-tools@13

      - name: Deploy Firebase Hosting, Functions, Firestore and Storage
        run: |
          firebase deploy --project presto-app-74abe \
            --only hosting,functions,firestore:indexes,storage \
            --force

      - name: Deploy Firestore rules (ignore 409 if rules unchanged)
        run: |
          output=$(firebase deploy --project presto-app-74abe --only firestore:rules --force 2>&1) && echo "$output" || {
            echo "$output"
            if echo "$output" | grep -q "409"; then
              echo "ℹ️  Firestore rules inchangées — release déjà existant, on continue."
              exit 0
            fi
            exit 1
          }
