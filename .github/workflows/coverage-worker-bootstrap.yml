name: Coverage Worker Bootstrap (paused)

# Bootstrap des workers de couverture mis en pause volontairement.
# Aucun déclenchement automatique (issues ou schedule).
# La logique précédente reste récupérable dans l'historique Git.
on:
  workflow_dispatch:

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Coverage Worker Bootstrap paused"
