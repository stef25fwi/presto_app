name: Temporary format admin bulk listing client

on:
  push:
    branches:
      - agent/phase-5-admin-bulk-listing-client

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase-5-admin-bulk-listing-client
          fetch-depth: 0

      - name: Setup Flutter 3.44.6
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.44.6'
          channel: stable
          cache: true

      - name: Format client files
        run: |
          dart format \
            lib/admin/shared/admin_bulk_listing_service.dart \
            test/admin/shared/admin_bulk_listing_service_test.dart

      - name: Commit formatted files
        run: |
          if git diff --quiet; then
            echo 'No formatting change.'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            lib/admin/shared/admin_bulk_listing_service.dart \
            test/admin/shared/admin_bulk_listing_service_test.dart
          git commit -m 'style: formate le client de suppression admin'
          git push origin HEAD:agent/phase-5-admin-bulk-listing-client
