name: Coverage Factory 5x2 (paused)

# Couverture mise en pause volontairement.
# Aucun déclenchement automatique (push, workflow_run ou schedule).
# La logique précédente reste récupérable dans l'historique Git.
on:
  workflow_dispatch:

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "Coverage Factory paused"
