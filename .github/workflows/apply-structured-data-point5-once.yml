name: Apply structured data point 5 once

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  completed:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Migration du point 5 déjà appliquée."
