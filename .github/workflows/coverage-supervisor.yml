name: LCOV Supervisor (paused)

# Supervision LCOV mise en pause volontairement.
# Aucun déclenchement automatique (schedule ou pull_request).
# La logique précédente reste récupérable dans l'historique Git.
on:
  workflow_dispatch:

jobs:
  paused:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: echo "LCOV Supervisor paused"
