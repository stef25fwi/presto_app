name: Search Console lot 15

on:
  schedule:
    - cron: '42 9 * * *'
  push:
    branches:
      - main
    paths:
      - '.github/workflows/search-console-lot15.yml'
      - 'tools/seo/fetch_search_console_metrics.mjs'
      - 'tools/seo/build_search_console_lot15_report.mjs'
      - 'tools/seo/google_service_account_auth.mjs'
      - 'config/seo-monitoring.json'
  workflow_dispatch:
    inputs:
      site_url:
        description: 'Propriété Search Console à privilégier'
        required: false
        default: 'sc-domain:ilipresto.fr'
        type: string

permissions:
  contents: read
  id-token: write
  issues: write
  statuses: write

concurrency:
  group: search-console-lot15
  cancel-in-progress: true

jobs:
  search-console-report:
    environment: recaptcha
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: '22'

      - name: Validate lot 15 contract
        run: node tools/seo/search_console_lot15_contract.test.mjs

      - name: Authenticate with Workload Identity Federation
        id: google_auth
        uses: google-github-actions/auth@v3
        with:
          workload_identity_provider: ${{ secrets.WIF_PROVIDER }}
          service_account: ${{ secrets.WIF_SERVICE_ACCOUNT }}
          token_format: access_token
          access_token_lifetime: 1800s
          access_token_scopes: https://www.googleapis.com/auth/webmasters.readonly

      - name: Build Search Console report
        id: report
        continue-on-error: true
        env:
          SEO_GOOGLE_ACCESS_TOKEN: ${{ steps.google_auth.outputs.access_token }}
          SEO_GSC_SITE_URL: ${{ github.event_name == 'workflow_dispatch' && inputs.site_url || 'sc-domain:ilipresto.fr' }}
        run: |
          node tools/seo/build_search_console_lot15_report.mjs \
            --output build/seo/search-console-lot15-report.json \
            --markdown build/seo/search-console-lot15-report.md \
            --enforce

      - name: Publish report summary
        if: always()
        run: |
          if [ -f build/seo/search-console-lot15-report.md ]; then
            cat build/seo/search-console-lot15-report.md >> "$GITHUB_STEP_SUMMARY"
          else
            echo '# Search Console automatisée — lot 15' >> "$GITHUB_STEP_SUMMARY"
            echo '' >> "$GITHUB_STEP_SUMMARY"
            echo 'Le rapport n’a pas pu être généré.' >> "$GITHUB_STEP_SUMMARY"
          fi

      - name: Upload Search Console evidence
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: search-console-lot15-${{ github.sha }}
          path: build/seo/search-console-lot15-report.*
          if-no-files-found: warn
          retention-days: 90

      - name: Publish commit status
        if: always()
        env:
          GH_TOKEN: ${{ github.token }}
          STATUS_STATE: ${{ steps.report.outputs.state }}
          STATUS_DESCRIPTION: ${{ steps.report.outputs.description }}
          TARGET_URL: ${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}
          COMMIT_SHA: ${{ github.sha }}
          REPOSITORY: ${{ github.repository }}
        run: |
          node <<'NODE' > /tmp/search-console-status.json
          const payload = {
            state: process.env.STATUS_STATE || 'error',
            target_url: process.env.TARGET_URL,
            description: String(process.env.STATUS_DESCRIPTION || 'Rapport Search Console indisponible').slice(0, 140),
            context: 'quality/search-console-lot15',
          };
          process.stdout.write(JSON.stringify(payload));
          NODE
          curl --fail-with-body --silent --show-error \
            --request POST \
            --header "Authorization: Bearer $GH_TOKEN" \
            --header 'Accept: application/vnd.github+json' \
            --header 'X-GitHub-Api-Version: 2022-11-28' \
            --data-binary @/tmp/search-console-status.json \
            "https://api.github.com/repos/$REPOSITORY/statuses/$COMMIT_SHA"

      - name: Open, update or close lot 15 issue
        if: always()
        uses: actions/github-script@v9
        with:
          script: |
            const fs = require('fs');
            const title = '[SEO lot 15] Search Console automatisée';
            const jsonPath = 'build/seo/search-console-lot15-report.json';
            const markdownPath = 'build/seo/search-console-lot15-report.md';
            const report = fs.existsSync(jsonPath)
              ? JSON.parse(fs.readFileSync(jsonPath, 'utf8'))
              : null;
            const markdown = fs.existsSync(markdownPath)
              ? fs.readFileSync(markdownPath, 'utf8')
              : '# Search Console automatisée — lot 15\n\nRapport absent.';
            const { data: issues } = await github.rest.issues.listForRepo({
              owner: context.repo.owner,
              repo: context.repo.repo,
              state: 'open',
              per_page: 100,
            });
            const existing = issues.find(
              (issue) => !issue.pull_request && issue.title === title,
            );
            const runUrl = `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}/actions/runs/${context.runId}`;
            const body = `${markdown}\n---\nWorkflow : ${runUrl}\n`;
            const healthy = report?.status === 'available';
            if (!healthy && existing) {
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.number,
                body,
              });
            } else if (!healthy) {
              await github.rest.issues.create({
                owner: context.repo.owner,
                repo: context.repo.repo,
                title,
                body,
              });
            } else if (existing) {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.number,
                body: `Accès Search Console validé : ${runUrl}`,
              });
              await github.rest.issues.update({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: existing.number,
                state: 'closed',
              });
            }

      - name: Enforce Search Console availability
        if: always()
        run: |
          node - <<'NODE'
          const fs = require('fs');
          const path = 'build/seo/search-console-lot15-report.json';
          if (!fs.existsSync(path)) process.exit(1);
          const report = JSON.parse(fs.readFileSync(path, 'utf8'));
          if (report.status !== 'available') process.exit(1);
          console.log(`Search Console available for ${report.siteUrl}`);
          NODE
