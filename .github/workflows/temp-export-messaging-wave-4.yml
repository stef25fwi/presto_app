name: Temporary messaging interaction coverage executor

on:
  pull_request:
    types: [opened, synchronize, reopened]
    branches: [main]

permissions:
  contents: write

jobs:
  implement-and-test:
    if: github.head_ref == 'coverage/messaging-thread-interactions-wave-4' && github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v7
        with:
          ref: ${{ github.head_ref }}
          fetch-depth: 0

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: 3.44.6
          channel: stable
          cache: true

      - name: Apply zero-line-growth messaging interaction refactor
        run: python3 tools/temp_messaging_wave4_patch.py

      - name: Format changed Dart files
        run: dart format lib/pages/messages/conversation_thread_page.dart test/pages/messages/conversation_thread_interaction_dialogs_coverage_test.dart

      - name: Enforce legacy page line budget
        shell: bash
        run: test "$(wc -l < lib/pages/messages/conversation_thread_page.dart)" -le 5182

      - name: Dependencies
        run: flutter pub get

      - name: Targeted messaging interaction tests
        shell: bash
        run: |
          set -o pipefail
          timeout 120s flutter test --reporter expanded test/pages/messages/conversation_thread_interaction_dialogs_coverage_test.dart 2>&1 | tee messaging-wave4-test.log

      - name: Targeted analyzer
        shell: bash
        run: |
          set -o pipefail
          flutter analyze lib/pages/messages/conversation_thread_page.dart test/pages/messages/conversation_thread_interaction_dialogs_coverage_test.dart 2>&1 | tee messaging-wave4-analyze.log

      - name: Upload messaging wave 4 diagnostics
        if: always()
        uses: actions/upload-artifact@v7
        with:
          name: messaging-wave-4-diagnostic-${{ github.run_id }}
          path: |
            messaging-wave4-test.log
            messaging-wave4-analyze.log
          retention-days: 2

      - name: Commit validated implementation and clean executor
        shell: bash
        run: |
          rm -f .github/workflows/temp-export-messaging-wave-4.yml
          rm -f tools/temp_messaging_wave4_patch.py
          rm -f messaging-wave4-test.log messaging-wave4-analyze.log
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "test(coverage): cover messaging interaction dialogs"
          git push origin HEAD:${{ github.head_ref }}
