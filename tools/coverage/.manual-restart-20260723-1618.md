# Coverage Factory manual restart

Requested on 2026-07-23 after closing stale worker missions #670 and #672.

This marker intentionally triggers `.github/workflows/coverage-agent.yml` on `main` so the factory measures the current LCOV baseline and prepares exactly two fresh worker missions.
