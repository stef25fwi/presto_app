include: package:flutter_lints/flutter.yaml

analyzer:
  exclude:
    - build_logs/**
    - audit_microia/**
    - backups/**
    - release_apk/**
    - downloads/**
    - node_modules/**
    - functions/node_modules/**
    - functions/lib/**
    - "**/*.g.dart"
    - "**/*.freezed.dart"

  errors:
    deprecated_member_use: ignore
    # Réactivation tentée le 15/08 puis annulée : flutter analyze signale des
    # occurrences qu'un détecteur statique maison n'avait pas vues. À reprendre
    # avec la sortie réelle de l'analyseur, voir docs/audit/audit-qualite-code-playstore-2026-08-15.md §2.
    use_build_context_synchronously: ignore
    strict_top_level_inference: ignore

linter:
  rules:
    avoid_print: false
    prefer_const_constructors: false
    prefer_const_literals_to_create_immutables: false
    prefer_final_fields: false
    prefer_final_locals: false
    prefer_final_in_for_each: false
    unnecessary_const: false
    unnecessary_this: false